apiVersion: v1
kind: Service
metadata:
    name: "akskoamano-041a"
    labels:
        app: "akskoamano-041a"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "akskoamano-041a"